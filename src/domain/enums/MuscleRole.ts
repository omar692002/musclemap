/**
 * The role a muscle plays in a given exercise.
 * Used everywhere instead of magic strings.
 */
export enum MuscleRole {
  Primary = 'PRIMARY',
  Secondary = 'SECONDARY',
  Stabilizer = 'STABILIZER',
}
