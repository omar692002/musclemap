/** A day of the training week. Programs are laid out over a Mon→Sun calendar so
 * the generator can space sessions for recovery (EM5). */
export enum Weekday {
  Mon = 'MON',
  Tue = 'TUE',
  Wed = 'WED',
  Thu = 'THU',
  Fri = 'FRI',
  Sat = 'SAT',
  Sun = 'SUN',
}
