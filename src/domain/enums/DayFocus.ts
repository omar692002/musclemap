/** A training day's focus — a translation key, resolved to a label in the UI. */
export enum DayFocus {
  Push = 'PUSH',
  Pull = 'PULL',
  Legs = 'LEGS',
  Upper = 'UPPER',
  Lower = 'LOWER',
  FullBody = 'FULL_BODY',
  ChestTriceps = 'CHEST_TRICEPS',
  BackBiceps = 'BACK_BICEPS',
  ShouldersCore = 'SHOULDERS_CORE',
}
