package com.musclemap.intel;

/** How a muscle group's recent weekly volume compares to its MEV/MAV/MRV landmarks. */
public enum TrainingStatus {
    UNTRAINED,
    UNDERTRAINED,
    OPTIMAL,
    OVERTRAINED
}
