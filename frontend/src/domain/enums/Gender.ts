/**
 * Profile gender. Values mirror the backend {@code com.musclemap.user.Gender}
 * vocabulary (persisted in {@code user_profiles.gender}).
 */
export enum Gender {
  Male = 'MALE',
  Female = 'FEMALE',
  Other = 'OTHER',
  Unspecified = 'UNSPECIFIED',
}
